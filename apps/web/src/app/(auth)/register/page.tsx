import { RegisterForm } from '@/features/auth/register-form';

export const metadata = {
  title: 'Register Workspace | Enterprise AI Web Platform',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
