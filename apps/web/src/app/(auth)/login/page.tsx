import { LoginForm } from '@/features/auth/login-form';

export const metadata = {
  title: 'Sign In | Enterprise AI Web Platform',
};

export default function LoginPage() {
  return <LoginForm />;
}
