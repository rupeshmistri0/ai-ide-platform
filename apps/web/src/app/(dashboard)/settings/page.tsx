import { SettingsView } from '@/features/settings/settings-view';

export const metadata = {
  title: 'Settings | Enterprise AI Web Platform',
};

export default function SettingsPage() {
  return <SettingsView />;
}
