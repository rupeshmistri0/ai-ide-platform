import { ChatView } from '@/features/ai-chat/chat-view';

export const metadata = {
  title: 'AI Chat Assistant | Enterprise AI Web Platform',
};

export default function AIChatPage() {
  return <ChatView />;
}
