[tool.poetry]
name = "ai-ide-api"
version = "0.1.0"
description = "FastAPI Backend for AI IDE Platform"
authors = ["Enterprise Dev"]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.100.0"
uvicorn = {extras = ["standard"], version = "^0.22.0"}

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
